% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/survivalHelpers.R
\name{calculatePairSurvival}
\alias{calculatePairSurvival}
\title{Calculate Survival Time and Status for a Matched Pair}
\usage{
calculatePairSurvival(
  matchedDataFinal,
  cohortBaseForOutcome,
  outcomeDatesCurrentOutcome,
  session = NULL
)
}
\arguments{
\item{matchedDataFinal}{A tibble containing the matched sets data, as
returned by \code{performPairMatching}. Must include \code{person_id}, \code{set_id},
\code{exposure_status}, \code{index_date}.}

\item{cohortBaseForOutcome}{A tibble representing the cohort that passed
outcome washout, used to retrieve observation periods (\code{obs_start_date},
\code{obs_end_date}) for the matched individuals.}

\item{outcomeDatesCurrentOutcome}{A tibble containing \code{person_id} and
\code{outcome_date} for the specific outcome being analyzed.}

\item{session}{Optional. The Shiny session object.}
}
\value{
A tibble ready for survival analysis. Includes \code{person_id}, \code{set_id},
\code{exposure_status}, \code{index_date}, \code{study_exit_date}, \code{time_to_outcome}
(in days), and \code{outcome_status} (1 if event occurred, 0 if censored).
Returns \code{NULL} if no valid survival data rows are generated.
}
\description{
Calculates the time-to-event (outcome) and event status for each person
in the matched cohort data. Time is calculated from the index date. Censoring
occurs at the outcome date or the end of the observation period, whichever
comes first after the index date.
}
\keyword{internal}
