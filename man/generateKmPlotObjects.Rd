% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/KaplanMeierAndCox.R
\name{generateKmPlotObjects}
\alias{generateKmPlotObjects}
\title{Generate Kaplan-Meier Plot Objects for Matched Pairs}
\usage{
generateKmPlotObjects(matchedSurvivalData, maxPlotTime = 1825, session = NULL)
}
\arguments{
\item{matchedSurvivalData}{A list structured generally like the output of
\code{calculateMatchedSurvivalData}. Each named element corresponds to an
exposure-outcome pair and is itself a list that must contain at least:
\verb{$survivalData} (a tibble/data frame with columns \code{time_to_outcome},
\code{outcome_status}, \code{exposure_status}) and descriptive character labels
\verb{$exposureLabel}, \verb{$outcomeLabel}.}

\item{maxPlotTime}{Numeric. The maximum time (in days, starting from index date)
to display on the x-axis of the Kaplan-Meier plots. Defaults to 1825 (~5 years).}

\item{session}{Optional. The Shiny session object. If provided, notifications
will be shown in the Shiny UI. Otherwise, messages are printed to the console.}
}
\value{
A list named by the unique exposure-outcome pair keys present in
\code{matchedSurvivalData}. Each element is a list containing:
\describe{
\item{\code{plotObject}}{The list object returned by \code{survminer::ggsurvplot}. This itself contains elements like \verb{$plot} (the ggplot KM curve) and \verb{$table} (the risk table plot).}
\item{\code{kmPValueInfo}}{The object returned by \code{survminer::surv_pvalue}, containing details about the log-rank test p-value.}
}
Returns an empty list if \code{matchedSurvivalData} is NULL or empty.
}
\description{
Creates Kaplan-Meier plots (with risk tables) using \code{survminer} and calculates
log-rank p-values for each exposure-outcome pair present in the matched
survival data. Adjusts plot limits dynamically.
}
\keyword{internal}
