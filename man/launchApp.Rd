% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/launch_app.R
\name{launchApp}
\alias{launchApp}
\title{Launch the Exposure2OutcomeSurv Shiny Application}
\usage{
launchApp(launch.browser = TRUE, ...)
}
\arguments{
\item{launch.browser}{Logical; if TRUE, the app will open in the system's
default web browser. Defaults to TRUE. Passed via the \code{options} argument
to \code{shiny::shinyApp}.}

\item{...}{Additional arguments passed as options to \code{shiny::shinyApp}
(e.g., \code{port}, \code{host}). See \code{?shiny::shinyApp} for details on available options.}
}
\value{
Invisibly returns the result of \code{shiny::shinyApp} (launches the app)
}
\description{
This function runs the main Shiny application interface for exploring
exposure-outcome survival relationships using matched cohort data from an OMOP CDM.
}
\details{
The function calls \code{shiny::shinyApp}, using the \code{app_ui} and \code{app_server}
functions defined within the 'Exposure2OutcomeSurv' package to create the application.
}
\examples{
\dontrun{
  # To run the application
  launchApp()

  # To run on a specific port and host
  # launchApp(port = 1234, host = "0.0.0.0")
}
}
